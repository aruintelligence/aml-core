# ĀML™ JavaScript API

The canonical JavaScript API reference is maintained at the repository root:

[**Open the current API reference →**](../API.md)

This compatibility page exists so older and external links to `docs/API.md` remain valid.

## Primary entry point

```js
import {
  compileAML,
  compileSource,
  generateAMLFromIntent,
  simulatePolicies,
  executeAccountableIntent,
  verifyExecutionReceipt,
  signExecutionReceipt,
  verifySignedExecutionReceipt
} from "aml-core";
```

For the current exported surface, examples, and signatures, use [API.md](../API.md) as the source of truth.
