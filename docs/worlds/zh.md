# 一分钟了解 ĀML

**状态：DRAFT — 本地化证明页面**

ĀML 是位于 AI/应用意图与屏幕像素之间的界面防火墙。

原型规则：

```text
render_allowed = restoration_value >= attention_cost
```

立即试用：
https://aruintelligence.github.io/aml-core/playground.html

把 `restoration_value` 调到低于 `attention_cost`，重新编译并检查决策。

然后打开 View Meaning：
https://aruintelligence.github.io/aml-core/view-meaning.html

ĀML 是一个可运行的原型，不是已批准的全球标准。
