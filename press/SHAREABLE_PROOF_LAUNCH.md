# Shareable ĀML Proof Launch

**Status: PITCH**

ĀML now has a proof state you can send as a URL.

This link opens directly to a suppressed interface state:

https://aruintelligence.github.io/aml-core/proof.html?attention=5&restoration=1

This one opens to the same interface state allowed:

https://aruintelligence.github.io/aml-core/proof.html?attention=5&restoration=5

Nothing is hidden behind a dashboard. The exact values are in the URL. The exact AML is visible. The decision is visible. The prototype rule is visible:

`render_allowed = restoration_value >= attention_cost`

The repository also proves deterministic receipt replay in CI and checks five ALLOW plus five SUPPRESS fixtures on every push.

This is a working prototype, not a claim that these scores objectively measure human cognition.

**Ask:** open the SUPPRESS link, change one number, copy the new link, screenshot the result, and file what happened.
