# ĀML Terminology

## EthicalRenderGate

The runtime evaluation system that determines whether an element deserves to render.

Core equation:

```aml
render_allowed = restoration_value >= attention_cost
